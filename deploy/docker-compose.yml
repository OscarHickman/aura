version: "3.8"

services:
  aura:
    build: ..
    image: aura:latest
    ports:
      - "80:5000"
    restart: unless-stopped
    environment:
      - AI_PAPERS_CONFIG=/app/config.yaml
    volumes:
      - ../user_credentials:/app/user_credentials:ro
